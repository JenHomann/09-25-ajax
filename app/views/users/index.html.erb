<h1>This is my user index page</h1>

<div class="clicker">
  <%= link_to("Click to view all users") %>
</div>

<div class="users_list">
  <ul>
  <% @users.each do |u| %>
    <li><%= u.name %></li>
    <% end %>
  </ul>
</div>

<p><%= link_to("Create a new user", new_user_path) %></p>